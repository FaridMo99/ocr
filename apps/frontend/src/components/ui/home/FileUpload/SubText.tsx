function SubText() {
  return (
    <p className="mt-5 max-w-xl text-base text-muted-foreground md:text-lg">
      Drop a file below. TextGrab extracts clean, copyable text in seconds — no
      formatting weirdness, no sign-up.
    </p>
  );
}

export default SubText;
